{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main BridgeHop window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-show"]
}
